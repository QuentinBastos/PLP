#include "parseur.h"
#include <stdio.h>
#include <stdlib.h>

ASTNode *parse_tokens(Token *t, int n) {
    // Dummy implementation as TP5 REPL doesn't use AST (?)
    return NULL; 
}
void free_ast(ASTNode *n) {}
